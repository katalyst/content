<%= turbo_stream.replace "content--editor--item-frame" do %>
  <%= render Katalyst::Content::Editor::NewItemsComponent.new(container: item.container) %>
<% end %>

<%= turbo_stream.replace dom_id(previous) do %>
  <%= render Katalyst::Content::Editor::ItemComponent.new(item:, container: item.container) %>
<% end %>

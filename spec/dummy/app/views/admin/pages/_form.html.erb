<%# locals: (page:) %>

<%= form_with(model: page, url: [:admin, page]) do |form| %>
  <%= form.govuk_error_summary %>
  <%= form.govuk_text_field(:title) %>
  <%= form.govuk_text_field(:slug) %>

  <%= form.button(type: :submit, class: "button") %>
<% end %>
